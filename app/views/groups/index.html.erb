<h1>Your Groups</h1>

<% @groups.each do |group| %>

<p>
  <%= link_to group.name, group %>
  <%= link_to "Edit", edit_group_path(group) %>
  <%= link_to "Delete", group, method: :delete, data: { confirm: "Delete group?"} %>
</p>

<% end %>

<p><%= link_to "Create a group", new_group_path %></p>
